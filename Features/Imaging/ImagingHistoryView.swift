import SwiftUI

struct ImagingHistoryView: View {
    var body: some View {
        List {
            Text("Saved findings will appear here.")
        }
        .navigationTitle("Imaging History")
    }
}
