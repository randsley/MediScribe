import SwiftUI

struct NotesHomeView: View {
    var body: some View {
        NavigationStack {
            List {
                Text("Notes module placeholder")
            }
            .navigationTitle("Notes")
        }
    }
}
