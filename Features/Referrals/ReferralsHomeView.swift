import SwiftUI

struct ReferralsHomeView: View {
    var body: some View {
        NavigationStack {
            List {
                Text("Referrals module placeholder")
            }
            .navigationTitle("Referrals")
        }
    }
}
